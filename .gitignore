# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
firebase-debug.log*
firebase-debug.*.log*

# Firebase cache
.firebase/

# Firebase config

# Uncomment this if you'd like others to create their own Firebase project.
# For a team working on the same Firebase project(s), it is recommended to leave
# it commented so all members can deploy to the same project(s) in .firebaserc.
# .firebaserc

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env

# dotenv variants and environment files (may contain secrets)
.env.*
functions/.env
functions/.env.*

# Firebase project and runtime config (may include project IDs or secrets)
.firebaserc
.runtimeconfig.json
functions/.runtimeconfig.json

# Package manager config files that can store registry tokens
.npmrc
.yarnrc
.yarnrc.yml
.pnpmrc

# Credentials, secrets and keys
secrets/
.secrets/
credentials/
.credentials/
.ssh/
id_rsa*
id_dsa*
id_ecdsa*
id_ed25519*
*.pem
*.key
*.p12
*.pfx
*.cer
*.crt

# Cloud provider local config (can contain auth tokens)
.gcloud/
.aws/
.azure/
.vercel/
.docker/

# Service account files
serviceAccount*.json
*service-account*.json
*-sa.json
*-secret*.json

# Mobile platform configs that can embed API keys
google-services.json
GoogleService-Info.plist

# Local databases / backups (may contain sensitive data)
*.sqlite
*.sqlite3
*.db
*.bak
*.backup

# Editor settings that may store tokens in settings
.vscode/settings.json
.vscode/*.code-workspace

# dataconnect generated files
.dataconnect
